# What a gateway is, in one file.
name: events_gateway

listen:
  addr: :8080
  max_body: 1MiB

  # Who may write. Required outside BREVIS_ENV=local, where the gateway
  # refuses to start without it.
  #
  # `keys_from` names the ENVIRONMENT VARIABLE holding the accepted keys,
  # comma separated. The keys are never in this file -- the same split
  # `secrets:` makes in a workflow, and for the same reason: the file is in git.
  #
  # Every key in the list works, which is what makes rotation possible: add the
  # new one, move the callers, remove the old.
  auth:
    type: bearer
    keys_from: BREVIS_INGEST_KEYS

# The Prometheus exposition, on its OWN address and never on the one above.
#
# That port is public by construction -- it is where clients POST -- so a
# /metrics on it would publish every stream name, path and destination to
# whoever finds the path. Naming the same address for both is refused at load.
#
# Leaving this out serves them on :9090 anyway: an ingestion service nobody can
# see is the failure this field exists against. `addr: ""` turns them off, and
# that is a decision somebody made rather than one they forgot.
metrics:
  addr: :9090

streams:
  - name: clicks
    path: /v1/clicks
    format: json            # json | array | ndjson -- declared, never sniffed

    # What becomes the ingestion_id. All four, because the formula is frozen
    # over exactly these: leaving one out produces a DIFFERENT id, not a
    # weaker one. provider and entity are the stream's; source_key and
    # record_ts name fields of the event.
    identity:
      provider: web
      entity: click
      source_key: event_id
      record_ts: occurred_at

    # A name in this binary's registry, not a path. Hooks are Go, compiled in.
    hook: enrich_clicks

    # Writes ingestion_loaded_at: when the gateway received it, UTC, RFC3339.
    # The SDK's column and the SDK's format, so a row this gateway lands and a
    # row a pipeline lands are the same shape. Opt-in, because it adds a field
    # and both a topic's subscribers and a table's columns notice.
    stamp_loaded_at: true

    # The claim check: an event too big is written somewhere WHOLE, and a
    # reduced version continues carrying a pointer back to it.
    #
    # A flat 413 loses the event, and an oversized payload is usually the most
    # interesting one somebody has -- the request with the whole document
    # attached, which is the case worth debugging.
    #
    # `larger_than` measures one EVENT, after the hook. listen.max_body is a
    # different thing: it caps a whole REQUEST and refuses with 413 before a
    # byte is parsed.
    #
    # `hook` names a second hook that returns the reduced event. Which fields
    # are heavy is domain knowledge and a YAML file cannot hold it. WITHOUT
    # one, the event is archived and dropped from the stream -- not lost,
    # because the archive has it whole, and counted, because an event that
    # silently stops arriving is the worst outcome here.
    oversize:
      larger_than: 256KiB
      archive:
        type: files
        path: gs://acme-oversize/clicks/
      hook: strip_heavy_clicks

    buffer:
      durability: memory    # the only tier implemented; `disk` is refused by name
      flush:
        every: 1s
        records: 500

      # Delivery is asynchronous and bounded. A request appends to the buffer
      # and returns; a full batch is handed to one of these workers, so no
      # caller ever waits for a publish, a COPY, or a sink that is down.
      #
      # workers     how many batches are delivered at once
      # queue       how many full batches may wait for a worker
      # max_records the ceiling on events held in memory for this stream.
      #             Past it the gateway answers 503 rather than accepting an
      #             event it has nowhere to put -- and the 503 is safe to act
      #             on: send the same body again, the ingestion_id is a frozen
      #             function of the event, so a retry is the same record.
      workers: 4
      queue: 64
      max_records: 10000

    sink:
      type: pubsub
      project: acme-prod
      topic: clicks
      # Fields of the EVENT that travel as message attributes. The topic's
      # contract belongs to whoever owns the topic, so nothing is invented here.
      attributes: [tenant, region]

    # How hard the sink is tried before a batch is given up on. Four attempts
    # over roughly seven seconds: enough to ride out a restart on the other
    # side, short enough that a sink which is down is known to be down.
    retry:
      attempts: 4
      backoff: 500ms
      max_backoff: 10s

    # Where a batch the sink will not take goes. Required, not defaulted: a
    # stream without one loses a refused batch to a log line, and an operator
    # who never chose that does not know it is happening.
    #
    # `files` reads a directory, gs:// and s3://, so this is a folder on a
    # laptop and a bucket in production with no change to the gateway.
    #
    # Each record carries _dead_letter_reason, _dead_letter_sink and
    # _dead_letter_at: whoever finds this file has the events and not the log,
    # and "why is this here" is their first question.
    dead_letter:
      type: files
      path: ./dead-letter/clicks/

  # The same events into a table, which is the other half of what a gateway is
  # for: one stream feeds whoever subscribes, another lands what somebody will
  # query.
  - name: orders
    path: /v1/orders
    format: ndjson          # one object per line: a batch in one request

    identity:
      provider: shop
      entity: order
      source_key: order_id
      record_ts: placed_at

    buffer:
      flush:
        every: 2s
        records: 1000

    sink:
      type: postgres

      # The NAME of the environment variable holding the connection string,
      # never the string: a DSN carries a password and this file is in git.
      dsn_from: BREVIS_ORDERS_DSN

      # Schema-qualified. The table must exist -- this does not create one,
      # because a service that creates tables turns a typo here into a second
      # table nobody is reading.
      table: landing.orders

      # Required, never defaulted. The two are genuinely different and only
      # the table's owner knows which it is:
      #
      #   append  COPY FROM STDIN, Postgres's fast path. Every delivery lands,
      #           including a redelivery. Right for a log, wrong for a table
      #           somebody counts.
      #   merge   the batch staged in a TEMP table and inserted with
      #           ON CONFLICT (ingestion_id) DO NOTHING, all in ONE
      #           transaction. The FIRST delivery wins and a redelivery is
      #           ignored. Needs a UNIQUE index on ingestion_id, and the
      #           driver refuses without one rather than silently appending:
      #
      #             CREATE UNIQUE INDEX CONCURRENTLY ON landing.orders (ingestion_id)
      #
      # `upsert` -- ON CONFLICT DO UPDATE, where the LAST delivery wins -- is
      # refused by name. It is a real mode nobody has implemented yet, and
      # accepting the word while behaving like `merge` is the failure this
      # field exists to prevent.
      write: merge

    dead_letter:
      type: files
      path: ./dead-letter/orders/

  # BigQuery. No dsn_from: it authenticates with the pod's own credentials,
  # which is what a workload identity is for. A connection string here would be
  # a second way to do what the platform already does.
  - name: telemetry
    path: /v1/telemetry
    format: array           # a JSON array of objects in one request

    identity:
      provider: device
      entity: reading
      source_key: reading_id
      record_ts: measured_at

    buffer:
      flush:
        every: 60s
        records: 2000

    sink:
      type: bigquery
      project: acme-prod
      dataset: landing
      # No dots: for BigQuery the project and the dataset are their own fields,
      # so "landing.telemetry" here would make a table with that literal name.
      table: telemetry
      write: merge

    dead_letter:
      # The dead letter in a bucket, which is what it should be in production:
      # written inside the image it would die with the container. Needs the
      # pod's GCP credentials, resolved at startup rather than on the first
      # batch it has to bury.
      type: files
      path: gs://acme-dead-letter/telemetry/

  # One route, N tables, nothing declared.
  #
  # A producer POSTs {table_name, ...} and the table is created if it is
  # absent. Every table auto_table creates has the SAME FOUR COLUMNS, so
  # creating one is a template rather than a decision:
  #
  #   ingestion_id  the identity, and the merge key
  #   ingested_at   arrival, OUR clock -- the partition column
  #   occurred_at   the producer's, when sent; NULL when not
  #   data          the event whole, as JSON
  #
  # One JSON column and not a column per field, which is the decision this is
  # built on. A new field is a new key: no DDL, no schema-change quota, no
  # write-stream reopen, no race between replicas -- and it is queryable the
  # day it arrives. It is also what the market converged on: Fivetran, Airbyte
  # and Snowpipe all land into a raw layer that cannot fail.
  - name: tables
    path: /v1/tables
    format: json

    # The identity here names the fields auto_table itself composes. The
    # producer sends no provider or entity: it sends a table name and a
    # document, and the id is a frozen UUID v5 over
    # auto_table|<table>|<idempotency_key or sha256(data)>|<occurred_at>.
    identity:
      provider: zarv
      entity: table
      source_key: table_name
      record_ts: occurred_at

    buffer:
      flush:
        # Sixty seconds, not one. BigQuery allows 1,500 load jobs per table
        # per DAY; a one-second window is 86,400, which is 57x the quota and
        # gone in about twenty-five minutes. The config refuses a window it
        # cannot honour rather than letting the first deploy find out.
        every: 60s
        records: 2000

    sink:
      type: auto_table

      # The FIELD of the event that says which table it belongs in. It names a
      # TABLE and never a dataset: a producer that can choose the dataset can
      # write into `gold`.
      table_from: table_name

      # The boundary a producer writes inside. Every field here is a refusal,
      # and that is the point: auto_table turns a string in a payload into
      # DDL, so the string is the whole of the attack surface.
      naming:
        # The default is the intersection of what Postgres, MySQL, BigQuery
        # and Redshift all accept unquoted, so a name that passes needs no
        # quoting anywhere and cannot carry an injection.
        pattern: '^[a-z][a-z0-9_]{2,48}$'
        allow: [app_, svc_]
        # A runaway producer stops at 20 rather than at BigQuery's
        # per-project quota, which is shared with everything else there.
        max_new_per_hour: 20

      # auto_table routes and does not write. `into` is what writes, and it
      # carries no `table`: the table comes from each event.
      into:
        type: bigquery
        project: acme-prod
        dataset: landing
        write: merge

    dead_letter:
      type: files
      path: ./dead-letter/tables/
